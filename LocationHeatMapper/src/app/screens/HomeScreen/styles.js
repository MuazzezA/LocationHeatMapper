import {StyleSheet} from 'react-native';
export default StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
    marginTop: 10,
    justifyContent: 'flex-end',
    alignItems: 'center',
  },
  map: {
    height: '100%',
    width: '100%',
  },
});
